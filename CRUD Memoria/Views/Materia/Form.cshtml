@model CRUD_Memoria.Models.Materia

@{
    ViewBag.Title = "Materia";
}


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <input type="hidden" id="operacion" value="@ViewData["Operacion"]" />

    <div class="form-horizontal">
        <h4>Materia</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @Html.LabelFor(model => model.Nombre, htmlAttributes: new { @class = "" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Nombre, new { htmlAttributes = new { @class = "" } })
                @Html.ValidationMessageFor(model => model.Nombre, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <br />
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a href="@Url.Action("Index", "Materia")" class="btn btn-secondary">Regresar</a>
            </div>
        </div>
    </div>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript">
        $(document).ready(function () {
            //deshabilitar los controles del formulario cunado la operacion es "ver"

            if ($("#operacion").val() == "ver") {
                $("form").find("input,select").attr('disabled', 'disabled');
            }

        });


    </script>
}