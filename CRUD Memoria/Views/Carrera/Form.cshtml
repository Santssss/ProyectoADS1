@model CRUD_Memoria.Models.Carrera

@{
    ViewBag.Title = "Mantenimiento Carrera";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <input type="hidden" id="operacion" value="@ViewData["Operacion"]" />

    <div class="form-horizontal">
        <h4>Carrera</h4>
        <hr />

        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Id)

        <div class="form-group">
            @Html.LabelFor(model => model.Codigo, htmlAttributes: new { @class = "" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Codigo, new { htmlAttributes = new { @class = "" } })
                @Html.ValidationMessageFor(model => model.Codigo, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Nombre, htmlAttributes: new { @class = "" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Nombre, new { htmlAttributes = new { @class = "" } })
                @Html.ValidationMessageFor(model => model.Nombre, "", new { @class = "text-danger" })
            </div>
        </div>
        <br />
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Guardar" class="btn btn-primary" />
                <a href="@Url.Action("Index", "Carrera")" class="btn btn-secondary">Regresar</a>
            </div>
        </div>
    </div>
}



@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script type="text/javascript">

        $(document).ready(function () {
            //deshabilitar los controles del formulario cunado la operacion es "ver"

            if ($("#operacion").val() == "ver") {
                $("form").find("input,select").attr('disabled', 'disabled');
            }

        });

    </script>
}

